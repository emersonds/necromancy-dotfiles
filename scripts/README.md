## Scripts
- `config-update.sh`: Updates repo config folders with local config folders.
> [!WARNING]
> `config-update.sh` is used to update the *config folders in the git repo*, **NOT** for updating *your config files*.
- `package-installer.sh`: This downloads all necessary packages. Inspect the script in your text editor of choice to verify it is installing the packages you want it to install.
